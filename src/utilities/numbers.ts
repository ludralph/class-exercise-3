// sum of numbers
const sum = (num1: number, num2: number) => {
    return num1 + num2;
}

const multiply = (num1: number, num2: number) => {
    return num1 * num2;
}

export default {
    sum,
    multiply
};